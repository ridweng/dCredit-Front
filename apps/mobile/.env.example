# NestJS API base URL
# In dev: your local machine IP (not localhost — Metro bundler runs on device/simulator)
EXPO_PUBLIC_API_URL=http://localhost:3001

# For device/simulator testing, use your machine's local IP:
# EXPO_PUBLIC_API_URL=http://192.168.1.X:3001
